{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. The app's own invoke commands (get_status, join_network, etc.) are ungated; this grants the one gated core API the UI needs: listening to backend events such as close-requested.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen"
  ]
}
